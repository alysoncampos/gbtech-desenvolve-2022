const salaPython = ['Alyson', 'João', 'Melissa', 'Helena'];

const salaJavaScript = ['Juliana', 'Ana', 'Jurema', 'Leo'];

const salasUnificadas = salaPython.concat(salaJavaScript);

console.log(salasUnificadas);
